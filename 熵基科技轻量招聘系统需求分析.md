# 熵基科技轻量招聘系统需求分析

> 文档版本：V1.4  
> 编写日期：2026-09-02  
> 产品定位：面向中小企业的企业招聘官网与轻量招聘管理系统  
> 技术方向：Java 后端（Spring Boot）+ Web 前端  
> 文档状态：需求基线（含关键技术决策）

## 变更记录

| 版本 | 日期 | 主要变更 |
|---|---|---|
| V1.0 | 2026-09-02 | 第一版需求基线 |
| V1.1 | 2026-09-02 | 补齐关键决策章节；认证定为服务端 Session；文件存储定为本地磁盘 + 鉴权下载；新增投递快照机制并重写数据实体；补全状态转换；明确工作台口径、在线简历完成度、投递前置条件；补齐接口、分页、错误码；性能指标改为可测口径；新增素材清单、术语表、待确认问题 |
| V1.2 | 2026-09-02 | **将撤回投递、多 HR 账号与职位归属、职位类别/工作地点后台管理、首页内容管理、CSRF Token 五项纳入第一版必做范围**。连带变更：新增 `WITHDRAWN` 状态与重投机制，投递唯一约束改为 `(candidate_id, job_id, attempt_no)`；HR 角色内新增管理员权限位，确立"读全局、写按归属"权限模型；新增 `site_content`、`site_asset` 实体与首页内容公开接口；新增站点图片的公开访问路径并与简历存储严格隔离；权限矩阵扩展为四列；错误码新增 CSRF、撤回、归属、字典类 |
| V1.3 | 2026-09-02 | **完全移除首页内容管理模块**。首页文案、企业数据与配图回归前端维护（配置文件 + 静态资源），随构建发布。连带变更：删除 `site_content`、`site_asset` 两张表与全部相关接口（`/api/public/site-content`、`/api/public/assets/{id}`、`/api/admin/site-content/**`、`/api/admin/site-assets/**`）；删除 HR 后台"首页内容管理"页面与站点图片库；系统内不再存在任何公开可访问的文件目录，简历成为唯一的文件类别；错误码删除 5003、5004；数据实体由 13 张表减为 11 张 |
| V1.4 | 2026-09-04 | 招聘官网顶部导航栏新增显式“首页”入口，点击后返回招聘首页；同步更新导航栏展示规则与验收标准。 |

## 0. 第一版定位与关键决策

第一版目标是把核心闭环完整跑通并可演示。决策原则：

- 凡是"加功能才能解决"且不影响核心闭环的问题，明确不做，并写清后果与人工兜底方式。
- 凡是"后期改动会导致数据模型或前后端重构"的问题，现在定死，即使实现很轻。

### 0.1 已定决策

| 编号 | 议题 | 决策 | 说明 |
|---|---|---|---|
| D1 | 认证方案 | 服务端 Session + HttpOnly Cookie | `logout` 语义真实，无需处理 token 刷新与黑名单 |
| D2 | 邮箱验证 | 不做 | 简化注册流程 |
| D3 | 手机号验证 | 不做短信验证，但投递前手机号必填并做格式校验 | 保证 HR 能联系到候选人 |
| D4 | 密码找回 | 不做自助找回，由管理员 HR 在后台重置 | 避免引入邮件服务，见 §8.5 |
| D5 | 通知能力 | 第一版无任何站外通知（邮件/短信/IM） | 面试通知由 HR 线下人工完成，见 §10.6 |
| D6 | 投递快照 | 必做：投递时冻结在线简历副本、附件文件版本、职位信息与候选人联系信息副本 | 见 §13.2 |
| D7 | 简历文件存储 | 本地磁盘 + 配置化根目录；数据库只存 `storage_key`；**禁止公开访问**，只能通过鉴权接口下载 | 见 §9.3、§15.5 |
| D8 | 撤回投递 | **必做**：候选人可在 `SUBMITTED`/`VIEWED`/`INTERVIEW` 状态撤回；撤回后可重新投递，同一职位累计最多 3 次 | 见 §9.6、§13.2 |
| D9 | 多 HR 账号与职位归属 | **必做**：HR 可多账号，职位有负责人；HR 内部分普通 HR 与管理员 HR（同一业务角色 + 权限位） | 见 §5.2、§10.8 |
| D10 | HR 权限模型 | **读全局、写按归属**：普通 HR 可查看全部职位与投递，但只能操作自己负责职位的数据；管理员 HR 无归属限制 | 见 §5.3、§10.3、§10.5 |
| D11 | 职位类别与工作地点 | **必做后台管理页**：支持新增、编辑、排序、启用/停用；不允许物理删除已被引用项 | 仅管理员 HR 可维护，见 §10.7 |
| D12 | 首页内容 | **不做后台管理**：首页文案、企业数据与配图由前端配置文件与静态资源维护，随构建发布 | 见 §7.2、§4.2 |
| D13 | 文件存储范围 | 系统内只有简历附件一类文件，存放于 `${upload.root}/resume/`，**不存在任何公开可访问的文件目录**；首页配图属于前端静态资源 | 见 §15.6、§16.2 |
| D14 | CSRF 防护 | **必做**：Spring Security CSRF，Cookie 下发 `XSRF-TOKEN`，写操作校验 `X-XSRF-TOKEN` 请求头 | 见 §16.2 |
| D15 | 面试轮次 | 单轮，`interview_info` 与投递 1:1；改期为更新原记录 | 与"不实现多轮面试"一致 |
| D16 | 面试结果与状态 | 面试结果填写后不自动变更投递状态，需 HR 显式点击结论 | 避免状态与结论不一致时无法修正 |

### 0.2 由决策带来的已知缺陷（第一版接受）

- 注册邮箱可能不存在或填错，系统无法识别（D2）。
- 用户忘记密码无法自助恢复，必须联系 HR 重置（D4）。
- 候选人不会收到任何面试通知，需要 HR 电话联系，或候选人自行登录查看（D5）。
- 无图形验证码，存在被脚本批量注册与批量上传文件的可能，仅有 §16.2 的最小限流。
- 同一候选人对同一职位的投递次数上限为 3 次，用尽后无法再投（D8）。
- 首页文案、企业数据与配图的调整需要改前端配置文件并重新构建发布，HR 无法自助修改（D12）。

## 1. 文档目的

本文档用于明确熵基科技轻量招聘系统第一版的建设目标、用户角色、功能范围、页面结构、业务流程、状态规则及验收标准，为后续页面原型、数据库设计、接口设计、编码、测试提供统一依据。

第一版围绕企业招聘的核心闭环展开：

```text
展示企业形象
→ 展示社会招聘和校园招聘职位
→ 求职者注册登录
→ 完善简历并投递
→ HR查看和处理投递
→ HR安排面试并更新结果
→ 求职者查看进度
```

## 2. 项目背景

传统企业官网中的招聘栏目通常只能展示职位，实际简历投递和后续处理可能依赖邮箱或第三方招聘平台，容易造成以下问题：

- 企业招聘品牌展示与实际投递流程割裂。
- 职位和简历数据分散，不便于统一管理。
- 求职者无法在企业官网中查看自己的投递进度。
- HR 需要在不同渠道之间手工整理简历和招聘状态。

本系统希望将"企业展示、职位浏览、简历投递和 HR 处理"集中到一个轻量系统中，同时保持较低的学习和使用成本。

## 3. 建设目标

### 3.1 业务目标

- 建设具有熵基科技品牌特色的招聘官网。
- 清晰区分社会招聘和校园招聘。
- 允许未登录用户自由浏览公司信息和职位。
- 仅在投递岗位时强制用户登录或注册。
- 为求职者提供简历维护、投递、撤回与进度查询能力。
- 为 HR 提供职位发布、简历筛选、面试处理和结果管理能力。
- 支持多名 HR 并行工作，职位有明确负责人，避免重复处理。

### 3.2 体验目标

- 官网首页具有较强视觉表现力，但不影响浏览和加载速度（量化约束见 §16.1）。
- 导航栏保持简洁，减少不必要入口。
- 登录和注册通过弹窗完成，不跳离当前页面。
- 注册只需最小信息，不引入任何验证码或验证环节。
- HR 后台操作路径短，主要任务在三步以内完成。
- HR 后台默认只呈现"我负责的"数据，需要时可切换到全局视图。
- 同一功能只保留一个清晰入口，避免重复菜单。

### 3.3 技术目标

- 前后端分离，接口遵循 REST 风格，统一响应结构与错误码（见 §15.8、§15.9）。
- 认证采用服务端 Session，并启用 CSRF Token 防护（D1、D14）。
- 角色与归属权限由后端统一校验，前端隐藏不作为权限手段。
- 关键业务状态由服务端控制，不允许前端任意修改。
- 简历附件是系统内唯一的文件类别，只能鉴权下载；不存在任何公开可访问的文件目录（D7、D13）。
- 投递记录保存简历与职位快照，保证历史投递内容不可被事后修改（D6）。
- 首页内容以前端配置文件维护，随构建发布，后端不承载首页内容（D12）。

## 4. 产品范围

### 4.1 第一版必须实现

#### 招聘官网

- 企业招聘首页（内容由前端维护，视觉分级见 §7.2.8）
- 社会招聘职位列表
- 校园招聘职位列表
- 职位搜索和筛选
- 职位详情
- 登录/注册弹窗

#### 求职者中心

- 注册、登录、退出
- 基本资料维护
- 在线简历维护
- 附件简历上传与替换
- 职位投递（含快照生成）
- **投递撤回与重新投递**
- 我的投递
- 投递进度与面试安排查看

#### HR 后台

- HR 登录
- 工作台（支持"我负责的 / 全部"视图切换）
- 职位新增、编辑、发布、关闭、撤回发布
- **职位负责人归属与转移**
- 社招/校招职位分类
- 投递记录查看和筛选（含撤回记录）
- 简历查看、下载和备注
- 招聘状态更新（含误操作撤销）
- 面试时间及方式记录
- 面试结果记录
- **职位类别管理**
- **工作地点管理**
- **HR 账号管理与密码重置**

#### 基础能力

- 简历附件的存储与鉴权下载（系统内唯一的文件类别）
- 统一响应结构、错误码与全局异常处理
- CSRF Token 防护
- 初始化脚本：管理员 HR 账号、职位类别与工作地点初始字典

### 4.2 第一版明确不实现

范围性排除：

- 独立面试官角色
- 多公司、多租户
- 部门级复杂权限（权限只到"角色 + 职位归属"这一层）
- 职位多级审批流
- 多轮面试
- Offer 在线签署与 Offer 管理
- AI 自动筛选简历
- 招聘渠道费用结算
- 内推奖励管理
- 视频面试系统
- 企业微信、钉钉、飞书集成
- 多语言和海外独立站
- 复杂招聘报表与人才库

流程性简化排除：

- 邮箱验证与激活（D2）
- 手机号短信验证码（D3）
- 图形验证码与机器人风控（仅保留 §16.2 的最小限流）
- 密码自助找回（D4，改为管理员 HR 后台重置）
- 邮件、短信、站内消息等任何通知能力（D5）
- **首页内容管理（D12）**：不提供首页文案、企业数据与配图的后台维护页面，也不提供站点图片库与图片上传能力；首页内容由前端配置文件与静态资源维护
- 候选人自助注销账号与自助导出个人数据（处理方式见 §16.5）

以上不实现项不得影响第一版核心流程交付。

## 5. 用户角色与权限

系统有两个业务角色：求职者和 HR。HR 内部通过权限位区分普通 HR 与管理员 HR，不构成第三种业务角色。

### 5.1 求职者

求职者可以：

- 浏览首页、企业介绍和招聘信息。
- 浏览社会招聘和校园招聘职位。
- 查看职位详情。
- 注册、登录和退出。
- 编辑自己的基本资料和简历。
- 上传自己的附件简历。
- 投递正在招聘的职位。
- **撤回符合条件的投递，并在次数允许时重新投递同一职位**（§9.6）。
- 查看自己的投递记录、当前进度和面试安排。

求职者不能：

- 查看其他求职者的信息或简历。
- 新增、修改或关闭职位。
- 修改自己的招聘处理状态。
- 查看 HR 的内部备注和面试评价。
- 撤回已出结论（`PASSED`/`REJECTED`）或已撤回的投递。
- 恢复已撤回的投递（只能重新投递，生成新记录）。

### 5.2 HR

#### 普通 HR

- 登录 HR 管理后台。
- 新增职位，新增后自动成为该职位负责人。
- 编辑、发布、关闭、撤回发布**自己负责的**职位。
- 查看全部职位与全部投递记录（只读）。
- 对**自己负责职位**的投递执行：查看/下载简历、填写内部备注、更新状态、安排面试、填写评价、撤销结论。
- 不能操作他人负责职位的投递，不能转移归属。
- 不能管理职位类别、工作地点和 HR 账号。

#### 管理员 HR

在普通 HR 的全部能力之上，额外可以：

- 操作任意职位与任意投递，不受归属限制。
- 转移职位负责人。
- 管理职位类别与工作地点字典（§10.7）。
- 新增、编辑、启用/停用 HR 账号，重置任意用户密码（§10.8）。

#### 账号约束

- HR 账号不能通过公开注册产生，只能由管理员 HR 在后台创建，或由初始化脚本创建。
- 初始化脚本创建的第一个 HR 账号即为管理员 HR，且系统必须始终保留至少 1 个启用状态的管理员 HR（删除/停用最后一个时拒绝，错误码 6003）。
- HR 账号不允许物理删除，只能停用；停用前必须先转移其名下所有职位（错误码 6002）。

### 5.3 权限矩阵

| 功能 | 未登录用户 | 求职者 | 普通 HR | 管理员 HR |
|---|---:|---:|---:|---:|
| 浏览招聘首页 | 允许 | 允许 | 允许 | 允许 |
| 查看社招/校招职位与详情 | 允许 | 允许 | 允许 | 允许 |
| 注册求职者账号 | 允许 | 不适用 | 不允许 | 不允许 |
| 编辑个人简历 | 不允许 | 仅本人 | 不适用 | 不适用 |
| 投递职位 | 需先登录 | 允许 | 不允许 | 不允许 |
| 撤回投递 | 不允许 | 仅本人且状态允许 | 不允许 | 不允许 |
| 查看个人投递 | 不允许 | 仅本人 | 不适用 | 不适用 |
| 下载简历附件 | 不允许 | 仅本人 | 全部 | 全部 |
| 新增职位 | 不允许 | 不允许 | 允许（自动成为负责人） | 允许 |
| 编辑/发布/关闭职位 | 不允许 | 不允许 | 仅本人负责 | 全部 |
| 转移职位负责人 | 不允许 | 不允许 | 不允许 | 允许 |
| 查看全部投递 | 不允许 | 不允许 | 允许（只读） | 允许 |
| 操作投递（状态/备注/面试） | 不允许 | 不允许 | 仅本人负责职位 | 全部 |
| 职位类别 / 工作地点管理 | 不允许 | 不允许 | 不允许 | 允许 |
| HR 账号管理与密码重置 | 不允许 | 不允许 | 不允许 | 允许 |

## 6. 信息架构与页面清单

### 6.1 招聘官网页面

```text
招聘官网
├── 首页
├── 社会招聘职位列表
├── 校园招聘职位列表
├── 职位详情
├── 我的简历
└── 我的投递
```

登录和注册不是独立页面，而是所有前台页面都可以调用的全局弹窗。

### 6.2 HR 后台页面

```text
HR后台
├── 工作台
├── 职位管理
│   ├── 职位列表（我负责的 / 全部）
│   └── 新增/编辑职位
├── 投递管理
│   ├── 投递列表（我负责的 / 全部）
│   └── 候选人投递详情
└── 系统配置（仅管理员HR可见）
    ├── 职位类别管理
    ├── 工作地点管理
    └── HR账号管理
```

"系统配置"菜单对普通 HR 完全不渲染，同时对应接口独立校验管理员权限。

## 7. 招聘官网需求

### 7.1 顶部导航栏

导航栏必须保持简洁。

未登录状态只显示：

```text
品牌标识 | 首页 | 社会招聘 | 校园招聘 | 登录
```

具体要求：

- 品牌标识位于最左侧，点击返回首页。
- “首页”“社会招聘”和“校园招聘”位于中间区域；点击“首页”返回招聘首页。
- "登录"按钮位于最右侧。
- 首页初始状态可以使用透明导航栏。
- 页面向下滚动后，导航栏变为半透明或毛玻璃背景，提高文字可读性。
- 首页导航栏不显示"热门职位""公司介绍""业务方向""我的投递"等额外菜单。

求职者登录后，最右侧的"登录"按钮替换为账号入口（姓名或头像）。点击后显示：

- 我的简历
- 我的投递
- 退出登录

HR 登录后，账号入口额外显示"进入 HR 后台"。

### 7.2 首页结构

首页承担企业品牌展示和社招/校招分流，不设置热门职位模块。

首页由七个滚动区块组成，**区块数量与顺序固定**。各区块的文案、企业数据与图片全部由前端配置文件与静态资源维护（D12），随构建发布。后端不提供首页内容接口，HR 后台不提供首页内容管理页。

| 区块 | 前端配置键 |
|---|---|
| 第一屏 品牌主视觉 | `hero` |
| 第二屏 认识熵基科技 | `about` |
| 第三屏 四大业务方向 | `business` |
| 第四屏 全球布局 | `global` |
| 第五屏 社招与校招入口 | `recruitEntry` |
| 第六屏 企业文化与工作体验 | `culture` |
| 第七屏 最终行动号召 | `cta` |
| 页脚 | `footer` |

各键的字段与下列各小节一一对应，具体默认值见前端配置文件。

#### 7.2.1 第一屏：品牌主视觉

展示内容：

- 熵基科技品牌标识。
- 企业核心宣传语（两行，前端配置）。
- 企业建筑、智能空间、AI 数据流等主视觉背景图（前端静态资源）。
- "探索社会招聘"和"加入校园招聘"两个行动按钮（按钮文案前端配置）。

默认文案：

```text
AI认知让空间更智能，让世界更美好
逐梦AI，与熵基一起成为 Change-Maker
```

交互要求：

- 点击社会招聘按钮进入社会招聘列表。
- 点击校园招聘按钮进入校园招聘列表。
- 允许使用轻量渐入、视差或粒子效果。
- 不使用强制开场动画，不阻塞用户操作。

#### 7.2.2 第二屏：认识熵基科技

展示：

- 企业定位和简要介绍。
- 企业园区、办公环境图片或宣传视频地址。
- 企业数据条目（如全球员工、研发人员、分支机构、覆盖国家和地区），由前端配置维护，建议 3–6 条，按数量自适应排列。

该区域只负责建立企业认知，不直接展示职位。

#### 7.2.3 第三屏：四大业务方向

固定展示 4 个业务方向，默认为智慧空间、数字身份认证、智慧办公、智慧商业，名称与内容由前端配置维护，**数量固定为 4 以保证布局**。

每个业务方向包含：

- 场景图片（前端静态资源，缺图时见 §17.2 占位规则）
- 业务名称
- 一句话业务介绍
- 可能涉及的岗位方向

#### 7.2.4 第四屏：全球布局

展示研发中心、制造中心、分支机构与全球业务覆盖。

实现约束：

- 底图为前端静态图片或内置静态 SVG 世界地图，不引入地图库和 WebGL。
- 布局要点以文字条目形式配置（如"研发中心 5 个"），不做坐标编辑。
- 发光节点、连线动画属于 P1 视觉增强，且仅在桌面端启用。
- 该区域提供"探索招聘机会"按钮，文案与跳转目标由前端配置。

#### 7.2.5 第五屏：社会招聘与校园招聘

采用左右分屏或上下分区，两侧标题、描述、按钮文案由前端配置：

- 社会招聘：强调工作经验、专业能力和价值创造。
- 校园招聘：强调成长、培养和职业起点。

每个区域必须包含独立按钮，分别进入对应职位列表。

#### 7.2.6 第六屏：企业文化与工作体验

展示：

- 企业价值观条目，默认为责任、正直、求实、卓越（由前端配置维护）。
- 图文卡片，默认为办公环境、学习成长、员工活动与福利（由前端配置维护，建议 3–4 张）。

#### 7.2.7 第七屏与页脚

第七屏再次呈现品牌招聘文案，并提供社会招聘和校园招聘入口。

页脚展示（由前端配置）：

- 公司地址
- 招聘联系邮箱与电话
- 隐私政策链接
- 使用条款链接
- 官方公众号二维码图片
- 备案信息

#### 7.2.8 首页交付分级

| 阶段 | 交付内容 |
|---|---|
| P0 | 七个区块结构 + 前端配置文件渲染 + 静态图片 + 基础渐入效果 + 社招/校招入口 |
| P1 | 视差、粒子、全球布局动态节点、宣传视频播放、区块进场动画 |

首页资源预算见 §16.1，超出预算的视觉效果一律降级到 P1。

### 7.3 社会招聘职位列表

只展示招聘类型为"社会招聘"且状态为"招聘中"的职位。

支持以下查询条件：

- 职位关键词（匹配职位名称，`LIKE '%kw%'`，第一版不做全文检索）
- 职位类别（来自 `job_category`，仅展示启用项）
- 工作地点（来自 `job_location`，仅展示启用项）

职位列表项显示：

- 职位名称
- 工作地点
- 职位类别
- 工作经验要求
- 学历要求
- 发布时间

列表默认按发布时间倒序，分页参数见 §15.8。点击职位进入职位详情。

### 7.4 校园招聘职位列表

只展示招聘类型为"校园招聘"且状态为"招聘中"的职位。

支持以下查询条件：

- 职位关键词
- 职位类别
- 工作地点

职位列表项显示：

- 职位名称
- 工作地点
- 职位类别
- 招聘对象（应届生 / 实习生，对应 `job.target_audience`）
- 学历要求
- 发布时间

### 7.5 职位详情

职位详情对未登录用户开放，展示：

- 职位名称
- 社会招聘或校园招聘标识
- 工作地点
- 招聘人数
- 职位类别
- 学历要求
- 工作经验要求（社招）或毕业年份要求与招聘对象（校招）
- 岗位职责
- 任职要求
- 发布时间
- "立即投递"按钮

立即投递规则，按顺序校验：

1. 职位必须处于招聘中状态，否则提示"该职位已停止招聘"（错误码 3001）。
2. 未登录时弹出登录/注册弹窗，登录成功后自动继续本职位的投递流程（见 §8.4）。
3. 已登录且为 HR 角色时，不显示投递按钮。
4. 存在未撤回的投递记录时，按钮显示为"已投递"，提示"您已投递该职位"（错误码 3002）。
5. 曾撤回且投递次数已达上限（3 次）时，按钮置灰，提示"该职位投递次数已达上限"（错误码 3007）。
6. 校验投递前置条件（§9.5），不满足时提示缺失项并提供跳转"我的简历"的入口（错误码 3003）。
7. 全部通过后展示投递确认弹窗，说明"提交后 HR 即可看到你的简历快照，撤回后同一职位最多可再投递 N 次"，用户确认后创建投递。

## 8. 登录与注册弹窗需求

### 8.1 基本要求

- 登录和注册共用一个全局弹窗，默认显示登录表单，可在两者间切换。
- 打开弹窗时，页面背景增加遮罩并禁止误操作。
- 关闭弹窗后保留当前页面位置和已填写的非敏感内容。
- 弹窗支持点击关闭按钮和遮罩关闭。
- 弹窗内不包含验证码、邮箱验证、短信验证任何环节（D2、D3）。
- 弹窗打开时若本地无 `XSRF-TOKEN`，先调用 `GET /api/auth/csrf` 获取（见 §16.2）。

### 8.2 登录

登录字段：邮箱、密码。

登录成功处理：

- 求职者登录后关闭弹窗并停留在当前页面。
- 如果登录前触发了"立即投递"，登录成功后继续投递流程。
- HR 登录成功后进入 HR 后台工作台。
- 账号处于停用状态时拒绝登录，提示"账号已停用，请联系管理员"（错误码 2006）。

登录失败处理：

- 在弹窗内显示明确错误信息，不关闭弹窗。
- 邮箱不存在与密码错误返回同一提示"邮箱或密码错误"（错误码 2003），不区分具体原因。
- 同一邮箱连续登录失败 5 次，锁定 10 分钟（错误码 2004）。

### 8.3 注册

公开注册只能创建求职者账号，不能选择 HR 角色。

注册字段（4 项 + 1 个勾选）：姓名、邮箱、密码、确认密码、同意隐私政策。

注册规则：

- 邮箱必须符合格式要求且全局唯一，重复时返回错误码 2001。
- 邮箱不做真实性验证，注册后立即可用（D2）。
- 密码长度为 8 至 20 位，不额外要求字符组合。
- 两次密码必须一致（错误码 2002）。
- 必须勾选同意隐私政策后才能提交。
- 密码使用 BCrypt 哈希存储，禁止明文保存。
- 注册成功后自动完成登录，并写入 `candidate_profile` 记录（姓名取注册值，其余为空）。
- 同一 IP 每小时注册次数上限见 §16.2。
- 后端忽略请求体中任何 `role` 字段，注册结果一律为 `CANDIDATE`。

### 8.4 待执行动作

当未登录用户点击"立即投递"时，前端保存待执行动作：

```text
actionType = APPLY_JOB
jobId = 当前职位ID
```

登录或注册成功后，系统按 §7.5 第 3 步起继续校验，用户不需要重新寻找职位。待执行动作在以下情况清除：用户手动关闭弹窗、动作执行完成、页面刷新。

### 8.5 密码重置

第一版不提供自助找回（D4），改为后台重置：

- 页脚"招聘联系方式"作为唯一求助入口。
- 管理员 HR 在"HR 账号管理"中可重置任意 HR 账号密码；求职者密码重置提供独立入口：按邮箱查询用户后重置（§10.8）。
- 重置结果为一个由系统生成的临时密码，由管理员线下告知用户；系统不发送任何通知。
- 重置操作必须记录操作人与时间（`sys_operation_log`）。
- 初始化部署文档需提供生成 BCrypt 哈希的命令示例，用于管理员账号本身丢失时的兜底恢复。

## 9. 求职者中心需求

### 9.1 基本资料

| 字段 | 是否必填 | 校验规则 |
|---|---|---|
| 姓名 | 必填 | 长度 2–20 |
| 手机号 | **投递前必填** | 匹配 `^1[3-9]\d{9}$`，不做短信验证（D3） |
| 邮箱 | 必填 | 默认取注册邮箱 |
| 性别 | 选填 | 男 / 女 / 不愿透露 |
| 所在城市 | 选填 | 自由文本 |

邮箱可修改，但不允许改为已被其他账号使用的邮箱（错误码 2001）。手机号在"我的简历"页面为选填保存（允许分次填写），但在投递校验时作为必填项（§9.5）。

### 9.2 在线简历

| 模块 | 是否必填 | 完成度判定 |
|---|---|---|
| 求职意向 | 必填 | 期望职位类别、期望工作城市均非空 |
| 教育经历 | 必填 | 至少 1 条，每条含学校、专业、学历、起止时间 |
| 工作或实习经历 | 选填 | — |
| 项目经历 | 选填 | — |
| 专业技能 | 选填 | — |
| 证书及获奖情况 | 选填 | — |
| 自我评价 | 选填 | — |

完成度判定规则（后端实现依据）：

```text
onlineResumeComplete =
    求职意向.期望职位类别 != null
 && 求职意向.期望工作城市 != null
 && 教育经历.size() >= 1
```

在线简历以 JSON 形式保存在 `resume.content`，支持保存草稿和多次编辑。编辑在线简历不会改变已提交投递的内容（§13.2）。

### 9.3 附件简历

- 支持 PDF、DOC、DOCX。
- 单个文件不超过 10MB。
- 后端必须校验文件大小、扩展名和文件头实际类型（Magic Number），三者不一致时拒绝（错误码 4001）。
- 文件名做安全处理，禁止路径穿越字符。
- 存储路径 `${app.upload.root}/resume/{candidateId}/{uuid}.{ext}`，数据库只保存 `storage_key`（D7）。
- 简历存储目录**不得**配置为静态资源映射目录，禁止任何形式的公开直链访问。

版本规则（D6 的一部分）：

- 每次上传生成一条新的 `resume_file` 记录，已有记录不可被修改或覆盖。
- 同一求职者只有一条记录标记 `is_current = 1`，作为"当前附件简历"。
- 上传新附件时旧记录 `is_current` 置 0，但文件与记录保留。
- 已提交的投递永久指向其投递时刻的 `resume_file_id`，不随当前附件变化。
- 求职者可取消当前附件（`is_current` 置 0），但不能删除已被投递引用的文件实体。

### 9.4 我的投递

列表显示：

- 投递职位（显示快照中的职位名称）
- 社招或校招
- 工作地点
- 投递时间
- 投递次序（第 N 次投递，仅当同一职位存在多次投递时显示）
- 当前状态（对外文案，见 §12.2）
- 面试信息（存在时）
- 操作：查看详情、撤回投递（仅在允许状态下显示）

要求：

- 求职者只看到对外状态，不显示 HR 内部备注和面试评价原文。
- 列表页固定显示提示："面试安排更新后不会发送通知，请留意本页面或 HR 的电话联系。"（D5 的补偿措施）
- 投递详情展示投递时的简历快照与职位快照，明确标注"此为投递时提交的版本"。
- 已撤回的记录保留在列表中，状态显示"已撤回"，不可再操作。

### 9.5 投递前置条件

后端在创建投递前必须校验（§7.5 第 6 步的唯一判定标准）：

```text
canApply = profileComplete && (hasCurrentResumeFile || onlineResumeComplete)

profileComplete       = 姓名非空 && 手机号匹配 ^1[3-9]\d{9}$
hasCurrentResumeFile  = 存在 is_current = 1 的 resume_file 记录
onlineResumeComplete  = 见 §9.2
```

校验失败时返回错误码 3003，并在 `data` 中返回缺失项列表（如 `["PHONE", "RESUME"]`），前端据此提示并跳转"我的简历"对应区块。

### 9.6 撤回投递（D8）

#### 允许条件

| 当前状态 | 是否允许撤回 | 说明 |
|---|---|---|
| SUBMITTED 已投递 | 允许 | — |
| VIEWED 处理中 | 允许 | — |
| INTERVIEW 待面试 | 允许 | 已安排的面试记录保留为历史，HR 端标注"候选人已撤回" |
| PASSED 已通过 | 不允许 | 错误码 3006 |
| REJECTED 暂不匹配 | 不允许 | 错误码 3006 |
| WITHDRAWN 已撤回 | 不允许 | 错误码 3006 |

#### 操作规则

- 入口位于"我的投递"列表与投递详情页，需二次确认，确认文案必须说明"撤回后本次投递记录仍会保留，HR 可见你已撤回"。
- 可选填撤回原因（下拉：已找到其他工作 / 投递错误 / 暂不考虑该岗位 / 其他 + 备注文本），写入 `application_log.remark`。
- 撤回后状态置为 `WITHDRAWN`，写入 `withdrawn_at`，并记录一条 `application_log`。
- 撤回不可逆，不能恢复为原状态；如需继续应聘，只能重新投递（生成新记录）。
- 撤回不删除快照，HR 仍可查看该次投递的简历快照（用于事后追溯）。

#### 重新投递规则

- 同一求职者对同一职位的投递记录总数上限为 **3 条**（即最多撤回 2 次后再投第 3 次）。
- 重新投递必须满足 §7.5 的全部常规校验，并**重新生成快照**（使用当时的简历与职位内容）。
- 重新投递的 `attempt_no = 该候选人该职位已有记录数 + 1`。
- 达到上限后再次投递返回错误码 3007。
- 存在任何未撤回记录（`status != WITHDRAWN`）时不允许新投递，返回错误码 3002。

#### HR 端表现

- 撤回记录在投递列表中默认**不显示**，需勾选"包含已撤回"筛选项后显示。
- 撤回记录为只读：不允许改状态、不允许填备注、不允许安排面试（错误码 3005）。
- 撤回记录不计入工作台任何指标（§10.2）。
- 同一候选人对同一职位存在多次投递时，投递详情页需展示"该候选人此前投递记录（含撤回）"的简要列表，便于 HR 判断。

## 10. HR 后台需求

### 10.1 HR 登录与认证

- 系统不设置独立的 HR 登录页面，HR 也通过招聘官网的登录弹窗登录。
- HR 账号由管理员 HR 创建或初始化脚本创建，不能公开注册（D9）。
- 后端根据 `sys_user.role` 识别 HR，根据 `sys_user.hr_admin` 识别管理员，登录成功后前端跳转至 HR 工作台。
- 未登录访问 HR 接口返回 401（错误码 1002）；已登录但非 HR 返回 403（错误码 1003）；HR 但非管理员访问管理员接口返回 403（错误码 1007）；HR 操作非本人负责数据返回 403（错误码 3008）。
- 前端隐藏菜单不作为权限手段，所有接口独立校验角色、管理员位与归属。

### 10.2 工作台

工作台支持视图切换 `scope`：

- `MINE`：只统计当前 HR 负责的职位及其投递（普通 HR 默认）。
- `ALL`：统计全部数据（管理员 HR 默认；普通 HR 可切换查看）。

四个指标口径（前后端以此为准，均排除 `WITHDRAWN`）：

| 指标 | 计算口径 |
|---|---|
| 招聘中职位数 | `job.status = PUBLISHED`，按 scope 过滤 `owner_hr_id` |
| 待处理投递数 | `job_application.status = SUBMITTED`，按 scope 过滤所属职位负责人 |
| 待面试数 | `status = INTERVIEW` 且关联 `interview_info.result IS NULL`，按 scope 过滤 |
| 已通过数 | `status = PASSED` 累计数，按 scope 过滤 |

工作台提供快捷入口：

- 发布职位
- 查看待处理投递（跳转投递列表并预置 `status=SUBMITTED` 与当前 scope）
- 查看待面试候选人（跳转投递列表并预置 `status=INTERVIEW` 与当前 scope）

管理员 HR 的工作台额外显示"未分配负责人的职位数"（仅在负责人被停用且未及时转移时可能出现，正常为 0）。

### 10.3 职位管理

#### 操作能力

| 操作 | 普通 HR | 管理员 HR |
|---|---|---|
| 新增职位 | 允许，自动成为负责人 | 允许，可指定负责人 |
| 编辑职位 | 仅本人负责 | 全部 |
| 发布 / 关闭 / 撤回发布 | 仅本人负责 | 全部 |
| 转移负责人 | 不允许 | 允许 |
| 查看职位列表与详情 | 全部（他人职位只读） | 全部 |

职位列表支持 `scope=MINE|ALL`（普通 HR 默认 `MINE`），并显示负责人姓名与对应投递数量（排除撤回）。

#### 职位字段

| 字段 | 社招 | 校招 | 说明 |
|---|---|---|---|
| 职位名称 | 必填 | 必填 | — |
| 招聘类型 | 必填 | 必填 | SOCIAL / CAMPUS |
| 职位类别 | 必填 | 必填 | 关联 `job_category`，只能选启用项 |
| 工作地点 | 必填 | 必填 | 关联 `job_location`，只能选启用项 |
| 负责人 | 系统/管理员维护 | 系统/管理员维护 | `owner_hr_id` |
| 招聘人数 | 必填 | 必填 | 正整数 |
| 学历要求 | 必填 | 必填 | 枚举 |
| 工作经验要求 | **必填** | 不适用 | 枚举，如 1-3 年 |
| 毕业年份要求 | 不适用 | **必填** | 如 2027 届 |
| 招聘对象 | 不适用 | **必填** | 应届生 / 实习生 |
| 岗位职责 | 必填 | 必填 | 富文本，输出前过滤 |
| 任职要求 | 必填 | 必填 | 富文本，输出前过滤 |
| 职位状态 | 系统维护 | 系统维护 | 见 §12.1 |
| 发布时间 | 系统维护 | 系统维护 | 首次发布时写入 |

按招聘类型的必填校验由后端执行：社招提交毕业年份/招聘对象、校招提交工作经验要求时，字段被忽略并置空，不报错。

#### 其他规则

- 职位不能物理删除，已产生投递的职位也不允许物理删除。
- 关闭职位后不再接受新投递，但历史投递仍可继续处理。
- 编辑招聘中的职位时 `job.version` 自增；已提交的投递保留其快照，不受影响。
- 转移负责人需二次确认，写入 `sys_operation_log`；转移后原负责人对该职位投递的写权限立即失效。

### 10.4 投递管理

投递列表支持：

- 视图切换 `scope=MINE|ALL`（普通 HR 默认 `MINE`）
- 按候选人姓名搜索
- 按职位筛选
- 按社招/校招筛选
- 按投递状态筛选
- "包含已撤回"开关（默认关闭，见 §9.6）
- 按投递时间排序

列表显示：

- 候选人姓名
- 手机号（列表页脱敏显示，如 `138****8000`）
- 投递职位
- 职位负责人
- 招聘类型
- 投递时间
- 投递次序（第 N 次）
- 当前状态
- 最近处理时间

非本人负责的记录，操作按钮置灰并提示"仅职位负责人可操作"（普通 HR）。

### 10.5 投递详情

HR 可以查看（全部投递均可查看）：

- 候选人基本资料（手机号在详情页明文显示）
- 投递时的在线简历快照
- 投递时的附件简历（在线预览或下载）
- 投递时的职位信息快照
- 职位当前信息（若与快照不一致，页面给出"职位已被修改"标识）
- 该候选人对本职位的历史投递列表（含已撤回）
- 投递时间、当前状态、职位负责人
- 状态变更记录（`application_log`，含操作人、时间、原状态、新状态、备注）
- 面试信息

HR 可以执行（普通 HR 仅限本人负责职位，管理员 HR 不限）：

- 标记已查看
- 设置待面试并填写面试安排
- 填写/修改内部备注（独立操作，不改变状态）
- 标记已通过
- 标记不合适
- 撤销上一次结论（`PASSED`/`REJECTED` → `VIEWED`）

危险操作需二次确认：标记不合适、撤销结论、关闭职位、转移负责人、停用 HR 账号、恢复首页默认内容。

已撤回的投递为只读，所有写操作被拒绝（错误码 3005）。

### 10.6 面试处理

第一版不建设独立面试官系统，也不做多轮面试（D15），HR 直接完成面试记录。

面试信息（与投递 1:1）：

| 字段 | 求职者可见 |
|---|---|
| 面试时间 | 可见 |
| 面试方式（线上/线下） | 可见 |
| 面试地点或会议链接 | 可见 |
| 联系说明 | 可见 |
| 面试评价 | **不可见** |
| 面试结果（PASS/FAIL） | 不直接可见，通过投递对外状态体现 |

规则：

- 面试改期为更新原记录，不新增记录；改期需写入一条 `application_log`。
- 填写面试安排时状态自动流转为 `INTERVIEW`。
- 填写面试结果后，HR 仍需显式点击"已通过"或"不合适"来变更投递状态，两者不自动联动（D16）。
- 第一版不向候选人发送任何面试通知（D5）。面试安排保存后，后台提示"系统不会通知候选人，请电话联系"，并展示候选人手机号。
- 候选人在 `INTERVIEW` 状态撤回后，面试记录保留，页面标注"候选人已撤回，面试作废"。

### 10.7 职位类别与工作地点管理（D11）

两个字典使用同一套交互，仅管理员 HR 可访问。

字段：

| 字段 | 说明 |
|---|---|
| 名称 | 必填，同一字典内唯一（错误码 5002），长度 1–20 |
| 排序值 | 整数，升序排列，默认追加到末尾 |
| 状态 | 启用 / 停用 |
| 引用数 | 只读，引用该项的职位数量 |

规则：

- 支持新增、编辑名称、调整排序值、启用/停用。
- **不允许物理删除**：引用数 > 0 时删除按钮不可用（错误码 5001）；引用数为 0 时也只提供"停用"，第一版不提供删除功能。
- 停用后：不再出现在官网筛选下拉与职位编辑下拉中；已引用该项的职位仍正常展示和处理（历史数据不受影响）。
- 职位新增/编辑时只能选择启用项；编辑一个引用了已停用字典项的职位时，该项在下拉中以"（已停用）"标注保留当前值，允许不改动保存。
- 官网筛选下拉只返回启用项（`GET /api/public/job-categories`、`/api/public/job-locations`）。
- 所有变更写入 `sys_operation_log`。

### 10.8 HR 账号管理（D9）

仅管理员 HR 可访问。

#### 账号列表与字段

| 字段 | 说明 |
|---|---|
| 姓名 | 必填 |
| 邮箱 | 必填，全局唯一，作为登录账号 |
| 是否管理员 | 是 / 否 |
| 状态 | 启用 / 停用 |
| 负责职位数 | 只读 |
| 创建时间、最后登录时间 | 只读 |

#### 操作规则

- **新增 HR**：填写姓名、邮箱、初始密码（或由系统生成临时密码），创建后即可登录；系统不发送任何通知，密码由管理员线下告知（D5）。
- **编辑 HR**：可改姓名、管理员位；邮箱可改但不能与其他账号重复。
- **停用 HR**：必须先将其名下所有职位转移给其他启用状态的 HR，否则拒绝（错误码 6002）；停用后该账号无法登录（错误码 2006），但其历史操作记录与 `application_log` 中的操作人信息完整保留。
- **不允许物理删除 HR 账号**（被 `job.owner_hr_id`、`application_log.operator_id` 引用）。
- **管理员保护**：系统必须始终保留至少 1 个启用状态的管理员 HR；停用或取消最后一个管理员的管理员位时拒绝（错误码 6003）。管理员不能取消自己的管理员位。
- **重置密码**：可重置任意 HR 账号密码；同时提供"按邮箱重置求职者密码"入口（§8.5）。重置后返回临时密码明文一次，仅在当次响应中显示，不落库明文。
- 所有账号操作写入 `sys_operation_log`。

## 11. 核心业务流程

### 11.1 浏览职位

```text
进入招聘首页（前端配置渲染七区块）
→ 选择社会招聘或校园招聘
→ 搜索/筛选职位（类别与地点来自启用字典）
→ 查看职位详情
```

整个过程不要求登录。

### 11.2 注册登录

```text
点击导航栏登录或点击立即投递
→ 前端确保已持有 XSRF-TOKEN
→ 打开登录/注册弹窗
→ 输入信息并提交（携带 X-XSRF-TOKEN）
→ 服务端校验（含账号状态）
→ 创建 Session 并写入 HttpOnly Cookie
→ 返回当前页面 / 继续投递 / 进入HR后台
```

### 11.3 求职者投递

```text
点击立即投递
→ 校验登录状态（未登录则弹窗登录后继续）
→ 校验职位状态为 PUBLISHED
→ 校验是否存在未撤回投递
→ 校验投递次数是否已达上限
→ 校验投递前置条件（§9.5）
→ 展示确认弹窗
→ 用户确认
→ 事务内：计算 attempt_no → 生成简历/职位/联系信息快照 → 写入 job_application → 写入 application_log
→ 返回投递成功结果
```

### 11.4 求职者撤回与重投

```text
在"我的投递"点击撤回
→ 校验归属（仅本人）与状态是否允许
→ 二次确认并可选填原因
→ 事务内：状态置 WITHDRAWN、写入 withdrawn_at、写入 application_log
→ 列表刷新，该记录变为只读
（如需继续应聘）
→ 重新进入职位详情投递
→ 校验次数上限（≤3）
→ 生成新记录与新快照
```

### 11.5 HR 处理

```text
HR登录 → 工作台（默认"我负责的"）
→ 查看待处理投递
→ 查看候选人简历快照与历史投递
→ 标记已查看
→ 判断是否进入面试
   ├── 否：标记不合适
   └── 是：填写面试安排 → 电话通知候选人
→ 完成面试并填写评价
→ 标记已通过或不合适
（结论填错时可撤销回"已查看"重新处理）
（候选人中途撤回时，该记录转为只读并从待办中消失）
```

### 11.6 管理员维护字典与账号

```text
管理员HR登录
→ 系统配置
   ├── 职位类别 / 工作地点：新增、排序、停用
   └── HR账号：新增HR、设置管理员位、转移职位后停用、重置密码
→ 所有变更写入操作日志
```
首页文案与配图不在后台维护，改前端配置文件后重新构建发布（D12）。

## 12. 状态设计

### 12.1 职位状态

| 状态编码 | 中文名称 | 是否官网展示 | 是否允许投递 |
|---|---|---:|---:|
| DRAFT | 草稿 | 否 | 否 |
| PUBLISHED | 招聘中 | 是 | 是 |
| CLOSED | 已关闭 | 否 | 否 |

允许转换：

```text
DRAFT      → PUBLISHED      发布
PUBLISHED  → CLOSED         关闭
PUBLISHED  → DRAFT          撤回发布，仅当该职位投递数为 0（含已撤回）
CLOSED     → PUBLISHED      重新发布，需二次确认
CLOSED     → DRAFT          不允许
```

说明：

- 重新发布不清空历史投递。此前投递过的候选人若已用尽 3 次投递次数，仍无法再投（§9.6）。
- 状态变更由后端校验，非法转换返回错误码 3005；无归属权限返回 3008。

### 12.2 投递状态

| 状态编码 | HR显示 | 求职者显示 | 是否终态 |
|---|---|---|---|
| SUBMITTED | 已投递 | 已投递 | 否 |
| VIEWED | 已查看 | 处理中 | 否 |
| INTERVIEW | 待面试 | 待面试 | 否 |
| PASSED | 已通过 | 已通过 | 是（HR 可撤销） |
| REJECTED | 不合适 | 暂不匹配 | 是（HR 可撤销） |
| WITHDRAWN | 已撤回 | 已撤回 | 是（不可变更） |

允许转换：

```text
SUBMITTED  → VIEWED | WITHDRAWN
VIEWED     → INTERVIEW | REJECTED | WITHDRAWN
INTERVIEW  → PASSED | REJECTED | WITHDRAWN
PASSED     → VIEWED            仅HR撤销误操作，需二次确认
REJECTED   → VIEWED            仅HR撤销误操作，需二次确认
WITHDRAWN  → 无                终态
```

约束：

- 转向 `WITHDRAWN` 的操作**只能由候选人本人发起**，HR 不能代为撤回。
- 其余所有转换只能由 HR 发起，且受归属校验约束（D10）。
- 任何状态都不允许回退到 `SUBMITTED`。
- `INTERVIEW` 同时覆盖"已约未面"和"已面待决定"，二者通过 `interview_info.result` 是否为空区分；工作台"待面试数"只统计 `result IS NULL` 的记录（§10.2）。第一版不为此新增状态。
- 状态变更必须写入 `application_log`，记录操作人、变更时间、原状态、新状态和备注。
- 状态变更采用条件更新（`WHERE id = ? AND version = ?`），版本不匹配时返回错误码 3004。

## 13. 核心业务规则

### 13.1 职位规则

- 只有 `PUBLISHED` 的职位可以在官网展示和投递。
- 职位关闭后详情仍可访问（用于历史链接），但不提供投递操作。
- 已有投递（含已撤回）的职位不允许物理删除，也不允许撤回发布。
- 社招和校招共用职位数据结构，通过 `recruitment_type` 区分，差异字段必填规则见 §10.3。
- 每个职位必须有且仅有一个负责人；负责人必须是启用状态的 HR。

### 13.2 投递规则

- 求职者必须登录后才能投递，HR 角色不能投递。
- 投递前置条件以 §9.5 的判定式为唯一标准。
- 同一求职者对同一职位：**同时最多存在 1 条未撤回记录**，历史记录总数最多 3 条。
- 数据库唯一约束（因支持撤回重投而调整）：

```sql
UNIQUE KEY uk_candidate_job_attempt (candidate_id, job_id, attempt_no)
```

- `attempt_no` 由服务端计算为"该候选人该职位已有记录数 + 1"。并发重复提交时两条记录会计算出相同 `attempt_no`，由唯一约束拦截；捕获 `DuplicateKeyException` 后返回错误码 3002，不返回 500。
- 应用层重复投递判定：存在 `status != WITHDRAWN` 的记录即拒绝（3002）；记录数已达 3 条则拒绝（3007）。
- 投递记录不可物理删除，撤回是唯一的"取消"手段。
- **投递快照规则（D6）**：创建投递时在同一事务内冻结以下内容，写入后永不修改：
  - `resume_snapshot`：在线简历 `content` 的 JSON 副本。
  - `resume_file_id`：指向投递时刻 `is_current = 1` 的 `resume_file` 记录；该记录与磁盘文件此后不可被覆盖或删除。
  - `job_snapshot`：职位名称、招聘类型、职位类别、工作地点、学历/经验/毕业年份/招聘对象要求、岗位职责、任职要求的 JSON 副本，以及当时的 `job.version`。
  - `candidate_snapshot`：投递时的姓名、手机号、邮箱。
- 撤回不清除快照；重新投递生成一份全新快照，与此前记录互不影响。
- HR 端一律展示快照内容；如需查看最新简历，需明确标注为"候选人当前简历"，且不覆盖快照。

### 13.3 数据可见性与归属规则

- 求职者只能查询和修改自己的资料与投递，接口层按 Session 中的 `userId` 过滤，不接受前端传入的 `candidateId`。
- 撤回接口必须校验该投递属于当前登录候选人，否则返回 403（1003）。
- HR 可查看全部职位与投递（读全局），但写操作按职位负责人校验（写按归属，D10），越权返回 3008。
- 管理员专属接口（字典、HR 账号）额外校验 `hr_admin`，越权返回 1007。
- HR 内部备注、面试评价字段不得出现在任何求职者端接口的响应体中（由独立 VO 保证，而非前端隐藏）。
- 简历附件只能通过鉴权接口下载；求职者仅能下载自己的文件，HR 可下载全部但必须经 `applicationId` 关联查询。
- 系统内不存在任何公开可访问的文件目录；简历目录严禁静态映射（D13）。
- 角色、管理员位与归属权限一律以服务端结果为准。

## 14. 核心数据实体

| 实体 | 主要用途 | 核心字段 |
|---|---|---|
| `sys_user` | 账号、角色与管理员位 | `id`、`email`(唯一)、`password_hash`、`role`(CANDIDATE/HR)、`hr_admin`、`status`(ENABLED/DISABLED)、`last_login_at`、`created_at` |
| `candidate_profile` | 求职者基本资料 | `id`、`user_id`(唯一)、`name`、`phone`、`gender`、`city` |
| `resume` | 在线简历（可变） | `id`、`candidate_id`(唯一)、`content`(JSON)、`updated_at` |
| `resume_file` | 附件简历（不可变、多版本） | `id`、`candidate_id`、`file_name`、`storage_key`、`file_size`、`content_type`、`is_current`、`uploaded_at` |
| `job_category` | 职位类别字典 | `id`、`name`(唯一)、`sort_order`、`status` |
| `job_location` | 工作地点字典 | `id`、`name`(唯一)、`sort_order`、`status` |
| `job` | 职位信息 | `id`、`title`、`recruitment_type`、`category_id`、`location_id`、`owner_hr_id`、`headcount`、`education`、`experience`、`graduation_year`、`target_audience`、`duty`、`requirement`、`status`、`version`、`published_at`、`created_by` |
| `job_application` | 投递记录（含快照） | `id`、`candidate_id`、`job_id`、`attempt_no`、`status`、`version`、`applied_at`、`withdrawn_at`、`last_handled_at`、`resume_snapshot`(JSON)、`resume_file_id`、`job_snapshot`(JSON)、`candidate_snapshot`(JSON)、`hr_note` |
| `application_log` | 投递状态与操作记录 | `id`、`application_id`、`action`、`from_status`、`to_status`、`operator_id`、`operator_type`(CANDIDATE/HR)、`remark`、`created_at` |
| `interview_info` | 面试安排与评价 | `id`、`application_id`(唯一)、`interview_time`、`method`、`address`、`contact_note`、`evaluation`、`result`、`updated_at` |
| `sys_operation_log` | 管理类操作审计 | `id`、`operator_id`、`module`、`action`、`target_id`、`detail`(JSON)、`created_at` |

关键约束（数据库设计阶段必须落实）：

```sql
UNIQUE KEY uk_email        (email)                              -- sys_user
UNIQUE KEY uk_user         (user_id)                             -- candidate_profile
UNIQUE KEY uk_cat_name     (name)                                -- job_category
UNIQUE KEY uk_loc_name     (name)                                -- job_location
UNIQUE KEY uk_cand_job_att (candidate_id, job_id, attempt_no)    -- job_application
UNIQUE KEY uk_application  (application_id)                      -- interview_info
INDEX idx_job_query        (recruitment_type, status, published_at)
INDEX idx_job_owner        (owner_hr_id, status)
INDEX idx_app_status       (status, applied_at)
INDEX idx_app_candidate    (candidate_id, job_id)
```

说明：

- `job_application.version` 与 `job.version` 用于条件更新，防止并发覆盖（§12.2、§18）。
- `operator_type` 用于区分候选人撤回与 HR 操作，`application_log` 因此可以承载两类主体。
- `hr_note` 放在 `job_application` 上（第一版只需一条备注），不单独建表。
- `resume_file` 不含可公开访问的完整 URL 字段，下载地址由鉴权接口按记录 id 解析。
- 详细字段类型、长度与外键在数据库设计阶段确定。

## 15. 接口需求概览

### 15.1 公开接口

```http
GET  /api/public/jobs?type=SOCIAL|CAMPUS&keyword=&categoryId=&locationId=&page=1&size=20
GET  /api/public/jobs/{id}
GET  /api/public/job-categories
GET  /api/public/job-locations
```

`job-categories` 与 `job-locations` 只返回启用项并按 `sort_order` 升序。

### 15.2 认证接口

```http
GET  /api/auth/csrf
POST /api/auth/register
POST /api/auth/login
POST /api/auth/logout
GET  /api/auth/me
```

`/api/auth/me` 返回 `userId`、`name`、`role`、`hrAdmin`，未登录返回 401。

### 15.3 求职者接口

```http
GET    /api/candidate/profile
PUT    /api/candidate/profile
GET    /api/candidate/resume
PUT    /api/candidate/resume
GET    /api/candidate/resume/file
POST   /api/candidate/resume/file
DELETE /api/candidate/resume/file
GET    /api/candidate/resume/file/download
GET    /api/candidate/jobs/{jobId}/apply-eligibility
POST   /api/candidate/jobs/{jobId}/apply
GET    /api/candidate/applications?status=&page=&size=
GET    /api/candidate/applications/{id}
POST   /api/candidate/applications/{id}/withdraw
GET    /api/candidate/applications/{id}/resume/download
```

`apply-eligibility` 返回是否可投、缺失项、已投递次数与剩余次数，用于前端按钮状态与提示文案。

### 15.4 HR 通用接口

```http
GET  /api/hr/dashboard?scope=MINE|ALL
GET  /api/hr/jobs?scope=&status=&type=&keyword=&page=&size=
POST /api/hr/jobs
GET  /api/hr/jobs/{id}
PUT  /api/hr/jobs/{id}
PUT  /api/hr/jobs/{id}/status
GET  /api/hr/applications?scope=&keyword=&jobId=&type=&status=&includeWithdrawn=false&page=&size=&sort=
GET  /api/hr/applications/{id}
GET  /api/hr/applications/{id}/logs
GET  /api/hr/applications/{id}/history
PUT  /api/hr/applications/{id}/status
PUT  /api/hr/applications/{id}/note
PUT  /api/hr/applications/{id}/interview
GET  /api/hr/applications/{id}/resume/download
GET  /api/hr/hr-users/options
```

- 所有 `/api/hr/**` 校验 `ROLE_HR`。
- 写类接口额外校验职位归属（管理员豁免），请求体必须携带 `version`。
- `applications/{id}/history` 返回该候选人对该职位的全部投递记录摘要（§10.5）。
- `hr-users/options` 返回启用状态 HR 的 `id` 与姓名，用于负责人下拉，普通 HR 也可调用（转移功能仍受管理员限制）。

### 15.5 管理员专属接口

```http
PUT    /api/hr/jobs/{id}/owner

GET    /api/admin/job-categories?page=&size=
POST   /api/admin/job-categories
PUT    /api/admin/job-categories/{id}
PUT    /api/admin/job-categories/{id}/status
GET    /api/admin/job-locations?page=&size=
POST   /api/admin/job-locations
PUT    /api/admin/job-locations/{id}
PUT    /api/admin/job-locations/{id}/status

GET    /api/admin/hr-users?page=&size=&status=
POST   /api/admin/hr-users
PUT    /api/admin/hr-users/{id}
PUT    /api/admin/hr-users/{id}/status
POST   /api/admin/hr-users/{id}/reset-password
POST   /api/admin/candidates/reset-password
```

所有 `/api/admin/**` 与 `PUT /api/hr/jobs/{id}/owner` 校验 `ROLE_HR` 且 `hr_admin = true`，否则返回 1007。

### 15.6 文件访问约定

简历文件（鉴权）：

- 下载接口返回文件流，`Content-Disposition: attachment`，文件名取 `resume_file.file_name`。
- `storage_key` 不出现在任何响应体中。
- 求职者只能下载 `candidate_id = 当前用户` 的文件；HR 必须通过 `applicationId` 关联查询，不允许按 `fileId` 直接下载。
- 文件不存在返回 1004，读取失败返回 4003。
- 简历目录不得通过任何静态映射或公开接口暴露。系统内不存在公开可访问的文件目录（D13）。

### 15.7 CSRF 约定

- `GET /api/auth/csrf` 下发 `XSRF-TOKEN` Cookie（非 HttpOnly），响应体同时返回 token 值。
- 所有 `POST`/`PUT`/`DELETE` 请求必须携带 `X-XSRF-TOKEN` 请求头，值与 Cookie 一致。
- `GET` 请求免校验；公开接口同样免校验（无状态变更）。
- 校验失败返回错误码 1006（HTTP 403），前端收到后自动重新获取 token 并允许用户重试一次。

### 15.8 分页与响应约定

统一响应结构：

```json
{ "code": 0, "message": "ok", "data": {} }
```

分页请求参数：`page`（从 1 开始，默认 1）、`size`（默认 20，最大 100）、`sort`（形如 `appliedAt,desc`）。

分页响应结构：

```json
{
  "code": 0,
  "message": "ok",
  "data": { "list": [], "total": 0, "page": 1, "size": 20 }
}
```

所有列表接口必须分页，禁止返回全量数据。字典下拉接口不分页，但返回量有上限。

### 15.9 错误码表

| code | HTTP | 含义 |
|---|---|---|
| 0 | 200 | 成功 |
| 1001 | 400 | 参数校验失败（`data` 返回字段路径） |
| 1002 | 401 | 未登录或会话已失效 |
| 1003 | 403 | 无权限访问 |
| 1004 | 404 | 资源不存在 |
| 1005 | 500 | 服务器内部错误 |
| 1006 | 403 | CSRF Token 缺失或不匹配 |
| 1007 | 403 | 需要管理员 HR 权限 |
| 2001 | 409 | 邮箱已被注册或被其他账号占用 |
| 2002 | 400 | 两次输入的密码不一致 |
| 2003 | 401 | 邮箱或密码错误 |
| 2004 | 429 | 登录失败次数过多，账号临时锁定 |
| 2005 | 429 | 操作过于频繁 |
| 2006 | 403 | 账号已停用 |
| 3001 | 409 | 职位不在招聘中 |
| 3002 | 409 | 已存在未撤回的投递，不可重复投递 |
| 3003 | 412 | 资料或简历不完整（`data` 返回缺失项） |
| 3004 | 409 | 记录已被更新（版本冲突） |
| 3005 | 409 | 状态转换不合法，或记录已撤回不可操作 |
| 3006 | 409 | 当前状态不允许撤回 |
| 3007 | 409 | 该职位投递次数已达上限 |
| 3008 | 403 | 非该职位负责人，无操作权限 |
| 4001 | 400 | 文件类型不支持 |
| 4002 | 413 | 文件大小超过限制 |
| 4003 | 500 | 文件存储或读取失败 |
| 5001 | 409 | 字典项已被职位引用，不可删除 |
| 5002 | 409 | 字典项名称重复 |
| 6001 | 409 | HR 邮箱已存在 |
| 6002 | 409 | 该 HR 仍负责职位，请先转移后再停用 |
| 6003 | 409 | 必须保留至少一个启用状态的管理员 HR |

## 16. 非功能需求

### 16.1 性能

性能指标为 P1 验收项，P0 阶段不作为阻塞条件。测量前需灌入种子数据：200 个职位、300 个候选人、2000 条投递、5 个 HR 账号。

| 指标 | 目标 |
|---|---|
| 职位列表、投递列表接口 | 10 并发下 P95 < 500ms（本地开发环境） |
| 职位详情、工作台接口 | 10 并发下 P95 < 300ms |
| 首页首屏图片资源总量 | ≤ 1.5MB |
| 首页 LCP（桌面宽带） | ≤ 2.5s |

实现约束：

- 职位与投递列表必须分页查询。
- 首页图片使用压缩与响应式尺寸，非首屏图片懒加载；首页内容由前端静态资源承载，不走后端接口。
- 超出资源预算的视觉效果降级至 P1（§7.2.8）。

### 16.2 安全

已实现项：

- 密码使用 BCrypt 哈希存储；重置密码不落库明文。
- 认证采用服务端 Session，Cookie 设置 `HttpOnly`、`SameSite=Lax`，生产环境加 `Secure`；闲置超时 2 小时。
- **CSRF Token 防护**（D14）：Spring Security CSRF + `CookieCsrfTokenRepository`，写操作校验 `X-XSRF-TOKEN`，失败返回 1006；同时保留 `SameSite=Lax` 与写操作 `Origin`/`Referer` 校验作为纵深防御。
- 简历上传三重校验（大小、扩展名、文件头）；简历下载经过归属与角色校验；简历目录不做静态映射。
- 富文本（岗位职责、任职要求）输出前做 XSS 过滤。
- 使用参数化查询防止 SQL 注入；关键参数由后端重新校验，不信任前端传值。
- 归属与管理员权限在服务层校验，不依赖前端菜单隐藏。
- 管理类操作全部写入 `sys_operation_log`。
- 最小限流：同一邮箱登录连续失败 5 次锁定 10 分钟；同一账号简历上传 ≤ 10 次/小时；同一 IP 注册 ≤ 10 次/小时；同一账号撤回操作 ≤ 20 次/小时（错误码 2004、2005）。

明确未实现项及风险：

| 未实现 | 风险 | 第一版兜底 |
|---|---|---|
| 图形验证码 | 可被脚本批量注册 | 最小限流 |
| 邮箱验证 | 存在无效邮箱账号 | HR 依赖手机号联系 |
| HTTPS 强制 | 开发环境明文传输 | 正式部署前必须补齐，且 `Secure` Cookie 依赖 HTTPS |
| 敏感字段加密存储 | 手机号明文入库 | 列表页脱敏 + 权限控制 |

### 16.3 易用性

- 未登录用户浏览职位时不强制弹出登录窗口。
- 登录弹窗错误信息必须靠近对应字段显示。
- HR 操作成功或失败后显示明确反馈。
- 危险操作需要二次确认：投递提交、撤回投递、关闭职位、标记不合适、撤销结论、转移负责人、停用 HR、字典停用。
- 无操作权限的按钮置灰并给出原因提示（如"仅职位负责人可操作"），不直接隐藏，避免 HR 误以为功能缺失。
- 空列表必须显示友好提示，而不是空白页面。
- 涉及"系统不发通知"的场景，页面必须显式告知（§9.4、§10.6、§10.8）。

### 16.4 响应式与兼容性

- 支持桌面端和移动端浏览招聘官网。
- HR 后台优先适配桌面端，同时保证平板端基本可用；字典管理允许仅桌面端可用。
- 复杂粒子和视差效果在移动端自动降级或关闭。
- 页面支持 `prefers-reduced-motion`。
- 目标浏览器：Chrome / Edge / Safari 最近两个大版本，不支持 IE。

### 16.5 数据合规

- 演示与测试环境只使用测试数据，不录入真实候选人简历；隐私政策页面需标注环境性质。
- 注册时的隐私政策勾选为必需项，但第一版不单独留存勾选证据（正式上线需补 `privacy_agreed_at` 字段）。
- 手机号在 HR 投递列表页脱敏显示，详情页明文（§10.4、§10.5）。
- 不提供候选人自助注销与数据导出（§4.2）；如有删除请求，由管理员执行数据库软删除并同步移除磁盘文件（已被投递引用的文件需一并处理）。
- 撤回投递不等于删除个人信息，撤回记录与快照仍保留，此点必须在隐私政策与撤回确认文案中说明。
- 数据留存期限、账号注销功能、个人信息收集告知书属于正式上线前必须补齐的事项，已列入 §24。

### 16.6 可维护性

- 前后端统一字段命名与状态枚举，枚举值以 §12 为准。
- 统一响应结构、错误码与全局异常处理（§15.8、§15.9）。
- 权限校验统一为三层切面：角色 → 管理员位 → 数据归属，不允许在各 Controller 内散落判断。
- 开发、测试、生产环境配置分离；文件存储根目录、Session 超时、上传大小限制均为配置项。
- 核心业务逻辑必须有测试：投递前置校验、重复投递与次数上限、撤回与重投、状态机合法性、快照生成、归属权限校验、管理员权限校验、CSRF 拦截（至少这八项）。
- 数据库变更通过版本化脚本管理，初始化脚本需包含管理员 HR 账号、职位类别与工作地点字典。

## 17. 视觉与交互原则

### 17.1 原则

首页可以借鉴优秀企业招聘网站的沉浸式视觉表达，但必须使用熵基科技自己的：

- 品牌标识
- 企业文案
- 业务场景
- 企业图片
- 蓝紫科技感或企业标准色
- AI、空间、数据流和端云连接视觉元素

不得直接复制其他公司的角色形象、图片素材、品牌配色或完整页面设计。

视觉效果必须服务于以下主路径：

```text
了解公司 → 选择社招/校招 → 查看职位 → 登录投递
```

### 17.2 素材依赖清单

现有素材（`熵基公司素材/`），复制到前端静态目录（如 `frontend/public/site/`）后由配置文件引用：

| 文件 | 用途 | 对应配置项 |
|---|---|---|
| `logo.png` | 导航栏、页脚、主视觉品牌标识 | 前端内置 |
| `company_profile_bg.jpg` | 第二屏背景 | `about.image` |
| `smart_office.jpg` | 智慧办公 | `business.items[].image` |
| `smart_space.jpg` | 智慧空间 | `business.items[].image` |

待补素材：

| 需求位置 | 素材 | 缺失时处理 |
|---|---|---|
| 第一屏 | 主视觉大图/短视频 | 复用 `company_profile_bg.jpg` + 渐变遮罩 |
| 第三屏 | 数字身份认证场景图 | 品牌色渐变块 + 图标占位 |
| 第三屏 | 智慧商业场景图 | 品牌色渐变块 + 图标占位 |
| 第四屏 | 世界地图 | 前端内置静态 SVG 世界地图 |
| 第六屏 | 办公环境、学习成长、员工活动 3 张 | 品牌色占位卡片 |
| 页脚 | 公众号二维码 | 灰色占位方块 |
| 全局 | Favicon | 由 `logo.png` 裁切生成 |

占位规则：

- 所有占位元素必须保持与最终素材一致的尺寸与宽高比，替换素材时不需要改动布局代码。
- 图片字段为空时，前端渲染占位样式，**不得报错或留白**。
- 素材齐备后替换前端静态资源并更新配置文件，重新构建发布即可，不需要改后端。

## 18. 异常与边界情况

| 场景 | 处理 | 错误码 |
|---|---|---|
| 会话过期后操作 | 弹出登录弹窗，保留当前页面与待执行动作 | 1002 |
| CSRF Token 失效（如长时间挂页） | 前端自动重新获取 token 并允许重试一次 | 1006 |
| 注册邮箱已存在 | 弹窗内提示，不关闭 | 2001 |
| 两次密码不一致 | 前端即时校验 + 后端二次校验 | 2002 |
| 连续登录失败 | 锁定 10 分钟并提示剩余时间 | 2004 |
| 已停用账号尝试登录 | 提示联系管理员 | 2006 |
| 职位在用户打开详情后被关闭 | 投递时后端拦截 | 3001 |
| 重复投递 / 并发重复提交 | 唯一约束拦截，返回友好提示而非 500 | 3002 |
| 资料或简历不完整 | 返回缺失项列表并跳转"我的简历" | 3003 |
| 撤回后再投且已达 3 次上限 | 按钮置灰并提示 | 3007 |
| 已出结论后候选人尝试撤回 | 拒绝并提示"该投递已有结论，无法撤回" | 3006 |
| 候选人撤回与 HR 改状态并发 | 条件更新，后者失败并提示刷新 | 3004 |
| HR 操作已撤回的投递 | 拒绝，记录只读 | 3005 |
| 普通 HR 操作他人负责职位 | 拒绝并提示"仅职位负责人可操作" | 3008 |
| 普通 HR 调用管理员接口 | 拒绝 | 1007 |
| 非法状态转换 | 后端拒绝 | 3005 |
| 停用 HR 时其名下仍有职位 | 拒绝并提示先转移 | 6002 |
| 取消最后一个管理员 | 拒绝 | 6003 |
| 停用被职位引用的字典项 | 允许停用，历史职位不受影响 | — |
| 删除被引用的字典项 | 拒绝 | 5001 |
| 首页某张配图缺失 | 前端按 §17.2 渲染占位，不白屏、不报错 | — |
| 上传文件类型/大小不符 | 拒绝并提示具体限制 | 4001 / 4002 |
| 附件上传中断或存储失败 | 事务回滚，不产生半条记录 | 4003 |
| 用户尝试查看/撤回他人投递 | 按当前用户过滤，越权返回 403 | 1003 |
| 快照引用的文件在磁盘缺失 | 提示"原始附件不可用"，不影响其他信息展示 | 1004 |

并发控制统一采用条件更新（`WHERE id = ? AND version = ?`）+ 事务，避免后提交的数据无提示覆盖先提交的数据。候选人撤回与 HR 状态变更共用同一套版本控制。

## 19. 验收标准

### 19.1 招聘官网

- 首页按七个区块完整展示，内容来自前端配置文件与静态资源，不请求后端内容接口。
- 首页某张配图缺失时按 §17.2 渲染占位，页面不白屏、不报错。
- 未登录状态导航栏只有品牌标识、首页、社会招聘、校园招聘、登录；点击“首页”可从任意招聘官网页面返回招聘首页；登录后最右侧为账号入口（含我的简历、我的投递、退出登录），HR 额外显示进入后台。
- 首页不出现热门职位模块。
- 社招和校招按钮正确进入对应职位列表。
- 未登录用户可以浏览职位列表和详情。
- 职位列表关键词、类别、地点筛选与分页均生效；类别与地点为字典下拉，只出现启用项。
- 校招列表正确显示"招聘对象"。

### 19.2 登录注册与 CSRF

- 登录和注册通过弹窗完成，不跳转独立页面，可在同一弹窗内切换。
- 注册只需姓名、邮箱、密码、确认密码与隐私政策勾选，全流程无验证码、无邮箱验证、无短信。
- 注册只能创建求职者账号；构造 `role=HR` 的请求被忽略。
- 点击投递后登录，成功时可以继续当前职位的投递流程。
- 重复邮箱、密码不一致、密码长度不符、账号密码错误、账号停用均有明确提示且错误码正确。
- 连续 5 次登录失败后账号被临时锁定。
- 退出登录后，携带原 Cookie 调用 `/api/auth/me` 返回 401。
- **不携带 `X-XSRF-TOKEN` 的 POST/PUT/DELETE 请求一律返回 1006**；携带错误 token 同样被拒绝；GET 请求不受影响。
- 从外部页面构造的跨站表单提交无法成功。

### 19.3 求职者

- 可维护基本资料和在线简历，支持草稿保存。
- 手机号未填写或格式错误时无法投递，提示明确。
- 在线简历未完成且无附件时无法投递；满足任一条件即可投递。
- 可上传 PDF/DOC/DOCX，超过 10MB 或伪造扩展名的文件被拒绝。
- 上传新附件后，旧附件不再显示为当前简历，但此前投递记录打开的仍是旧附件（快照验收要点）。
- 修改在线简历后，此前投递记录展示的内容不变（快照验收要点）。
- 存在未撤回投递时无法重复投递同一职位。
- 只能投递招聘中的职位。

### 19.4 撤回与重新投递

- `SUBMITTED`、`VIEWED`、`INTERVIEW` 状态下可见撤回入口并可成功撤回；撤回需二次确认。
- `PASSED`、`REJECTED`、`WITHDRAWN` 状态下无撤回入口，直接调用接口返回 3006。
- 撤回后记录状态显示"已撤回"，变为只读，且从 HR 待办与工作台指标中消失。
- 撤回后可以重新投递同一职位，生成第 2 条记录，`attempt_no = 2`，并生成新的快照。
- 第 3 次投递后再撤回，第 4 次投递被拒绝，返回 3007，按钮置灰。
- 撤回原因（如填写）出现在 `application_log` 中，`operator_type = CANDIDATE`。
- 候选人无法撤回他人投递（构造 `applicationId` 返回 403）。
- HR 无撤回入口，且无法通过状态接口将投递置为 `WITHDRAWN`。
- HR 投递列表默认不显示撤回记录，勾选"包含已撤回"后显示。
- 在 `INTERVIEW` 状态撤回后，HR 端面试记录仍在并标注"候选人已撤回"。
- 候选人撤回与 HR 同时改状态时，后提交者收到 3004。

### 19.5 多 HR 与职位归属

- 管理员 HR 可创建新 HR 账号，新账号可立即登录。
- 普通 HR 新建职位后自动成为负责人；职位列表默认只显示自己负责的，可切换到全部。
- 普通 HR 可以查看他人负责的职位与投递，但所有写操作返回 3008，按钮置灰并提示原因。
- 管理员 HR 可操作任意职位与投递，可转移职位负责人；转移后原负责人写权限立即失效。
- 工作台在 `MINE` 与 `ALL` 下的数字符合 §10.2 口径，且均排除已撤回记录。
- 普通 HR 访问任意 `/api/admin/**` 接口返回 1007，前端"系统配置"菜单不渲染。
- 停用仍负责职位的 HR 被拒绝（6002）；转移后可成功停用；被停用账号无法登录（2006），但其历史操作记录中的姓名仍可正常显示。
- 取消或停用最后一个管理员 HR 被拒绝（6003）；管理员不能取消自己的管理员位。

### 19.6 字典管理

- 管理员可新增、编辑、排序、停用职位类别与工作地点。
- 名称重复时被拒绝（5002）。
- 停用后该项不再出现在官网筛选下拉与职位新建下拉中。
- 引用了已停用字典项的历史职位仍能在官网正常展示，编辑该职位时下拉保留当前值并标注"（已停用）"。
- 界面不提供物理删除；引用数正确显示。
- 排序值调整后，官网下拉顺序同步变化。

### 19.7 数据与安全

- 密码不以明文形式存储（查库验证为 BCrypt 哈希）；重置密码返回的临时密码不落库明文。
- 数据库中不存在可直接访问的简历公开 URL。
- 求职者 A 无法通过修改 `applicationId` 下载或撤回求职者 B 的投递。
- 关键业务状态由后端校验，前端篡改请求参数无效。
- 重复或并发投递只能产生一条有效记录。
- 关闭职位不会删除历史投递数据。
- 所有管理类操作（字典、HR 账号、归属转移、密码重置）在 `sys_operation_log` 中可追溯到操作人与时间。
- 初始化脚本可一键创建管理员 HR 与字典数据。
- 已下线的首页内容接口（`/api/public/site-content`、`/api/public/assets/{id}`、`/api/admin/site-content/**`、`/api/admin/site-assets/**`）返回 1004。
- **简历文件无法通过任何公开路径或静态映射访问**（安全验收要点）。

## 20. 开发优先级

### P0：第一版必做

1. 项目基础：统一响应、错误码、全局异常、Session 认证、CSRF、三层权限切面（角色 / 管理员位 / 归属）
2. 注册、登录、退出
3. HR 账号管理与密码重置
4. 职位类别与工作地点管理（含初始化字典）
5. 文件存储：简历鉴权下载（系统内唯一的文件类别）
6. HR 职位管理（含负责人归属与转移）
7. 社招和校招职位列表与筛选分页
8. 职位详情
9. 求职者资料与简历（在线 + 附件）
10. 职位投递（前置校验、快照生成、重复与次数校验）
11. 投递撤回与重新投递
12. HR 投递处理（状态机、备注、面试安排、结论撤销、归属校验）
13. 求职者查看投递状态与面试安排
14. 首页静态版（七区块结构，内容由前端配置与静态资源维护）

### P1：体验完善

1. 首页视觉增强（视差、粒子、全球布局动效、视频）
2. 性能指标达标（§16.1）
3. 邮件通知（面试安排、投递结果、密码重置）
4. 密码自助找回
5. 操作日志页面化展示
6. 移动端细节打磨

### P2：后续扩展

1. 对象存储替换本地磁盘
2. 招聘统计报表
3. 人才库
4. Offer 管理
5. 多轮面试与独立面试官角色
6. 部门级权限与职位协作人
7. 第三方平台集成
8. 账号注销与个人数据导出（正式上线前必须完成）

## 21. 建议开发顺序

```text
需求基线确认
→ 页面原型确认（新增：字典管理、HR账号管理两页）
→ 数据库设计（重点：快照字段、attempt_no 唯一键、owner_hr_id）
→ 接口设计（统一响应、错误码、分页契约、CSRF 约定）
→ Spring Boot基础工程
→ Session认证 + CSRF + 三层权限切面
→ HR账号管理（先有账号才能测归属）
→ 字典管理
→ 文件存储能力（简历鉴权下载）
→ 职位管理（含归属）
→ 社招/校招职位展示
→ 简历管理
→ 投递业务（含快照）
→ 撤回与重投
→ HR处理流程（含归属校验）
→ 首页静态版渲染（前端配置 + 静态资源）
→ 首页视觉增强
→ 测试与部署
```

## 22. 第一版完成定义

满足以下条件时，第一版可视为完成：

1. 未登录用户能够从首页进入社招或校招并查看职位，首页内容由前端配置与静态资源渲染。
2. 用户点击投递时能够在弹窗中完成注册或登录，全流程无验证码与验证环节。
3. 求职者能够维护简历并成功投递职位，投递前置校验按 §9.5 生效。
4. 投递记录保存了简历、附件、职位与联系信息快照，候选人事后修改简历不影响 HR 看到的内容。
5. 存在未撤回投递时无法重复投递；候选人可撤回并在 3 次上限内重新投递，撤回记录保留且对 HR 只读。
6. HR 能够发布职位、查看简历、下载附件、安排面试、更新处理结果并撤销误操作。
7. 系统支持多个 HR 账号，每个职位有明确负责人；普通 HR 只能操作自己负责的数据，越权被后端拒绝。
8. 管理员 HR 能够管理 HR 账号、职位类别与工作地点，并可转移职位归属；所有管理操作可审计。
9. 求职者能够在账号入口中查看自己的投递进度与面试安排。
10. 所有写接口受 CSRF Token 保护，简历文件无法被公开访问。
11. 首页具有熵基科技品牌特色，占位素材不影响结构完整性，素材齐备后改前端静态资源并重新发布。
12. §4.2 中所有"不实现"项确实未实现，且其后果在界面上有对应提示或人工兜底说明。

## 23. 术语表

| 术语 | 含义 |
|---|---|
| 社招 / SOCIAL | 社会招聘，面向有工作经验的候选人 |
| 校招 / CAMPUS | 校园招聘，面向应届生与实习生 |
| 投递 / application | 求职者对某个职位提交的一次申请 |
| 投递次序 / attempt_no | 同一候选人对同一职位的第 N 次投递，上限 3 |
| 撤回 / WITHDRAWN | 候选人主动终止本次投递，记录保留且不可恢复 |
| 快照 / snapshot | 投递时冻结的简历、附件、职位与联系信息副本，写入后不可修改 |
| 当前附件简历 | `resume_file` 中 `is_current = 1` 的记录，仅用于展示与新投递 |
| 职位负责人 / owner | `job.owner_hr_id` 指向的 HR，拥有该职位及其投递的写权限 |
| 管理员 HR / hr_admin | 具备管理员权限位的 HR，可管理账号、字典与归属 |
| 读全局、写按归属 | 普通 HR 可查看全部数据，但只能操作自己负责职位的数据 |
| 视图范围 / scope | 列表与工作台的数据范围，`MINE` 或 `ALL` |
| 对外状态 | 求职者端看到的状态文案，与 HR 端状态编码一对一映射（§12.2） |
| 内部备注 / hr_note | 仅 HR 可见的文本，不出现在求职者端任何接口 |
| 前置校验 | §9.5 定义的投递资格判定式 |
| 版本冲突 | 条件更新因 `version` 不匹配而失败，返回 3004 |

## 24. 待确认问题

以下问题不阻塞第一版开发，但需在正式上线前确认：

| 编号 | 问题 | 影响 |
|---|---|---|
| Q1 | 简历数据留存期限与到期处理方式 | 合规必需，影响是否需要定时清理任务 |
| Q2 | 是否提供候选人自助注销与数据导出 | 合规必需，影响 `sys_user` 软删除设计 |
| Q3 | 个人信息收集告知书与隐私政策的正式文本 | 由法务提供，当前为占位 |
| Q4 | 正式环境是否启用邮件通知，使用哪个 SMTP/服务商 | 影响 P1 排期，也影响密码重置体验 |
| Q5 | 正式环境部署形态（单实例 or 多实例） | 多实例必须替换为对象存储，且 Session 需外置（Redis） |
| Q6 | 同一职位投递次数上限是否为 3 次，是否需要撤回冷却期 | 影响 §9.6 规则与前端提示文案 |
| Q7 | 是否需要"职位协作人"（多个 HR 共同处理一个职位） | 当前为单负责人，若需要则要引入关联表 |
| Q8 | 职位类别与工作地点的初始字典内容 | 需业务方提供清单用于初始化脚本 |
| Q9 | 熵基科技品牌规范（标准色值、字体、Logo 使用规范） | 影响首页视觉实现 |
| Q10 | 待补素材的提供时间 | 见 §17.2，逾期则以占位方案交付 |
